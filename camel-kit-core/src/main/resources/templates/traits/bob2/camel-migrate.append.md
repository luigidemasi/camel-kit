## Agent Optimization: IBM Bob 2

Switch to `camel-brainstorm-mode` before migration discovery and design. Use `explore` subagents only for
self-contained source analysis; keep the interview, design decisions, and approval gate in the parent task.

For a pipeline knowledge lookup, load `.bob/personas/knowledge-researcher.md` and spawn `camel-reviewer` with that complete role and query.
