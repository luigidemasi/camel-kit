---
name: camel-flow
description: Use this skill when the user wants to create a NEW integration from scratch — a greenfield project with no existing code to migrate. Trigger for 'new route', 'new integration', 'new flow', 'build from scratch', 'connect X to Y' (when no existing project), 'design a new pipeline', 'create a Camel project', 'I want to integrate', 'set up a new data flow', or any request to build something new involving message routing, data transformation, or system connectivity. This is a shortcut into the design phase for greenfield projects.
user_invocable: true
---

# Camel Flow — Greenfield Entry Point

Shortcut into `camel-brainstorm` with project type pre-set to **greenfield**.

Invoke `camel-brainstorm/SKILL.md` with:
- Project type: **greenfield** (skip detection question)
- Load `camel-brainstorm/guides/greenfield-interview.md` directly after context loading
