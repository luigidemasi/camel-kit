## Agent Optimization: Google Antigravity

Keep the interview and all user decisions in the primary conversation. For bounded catalog or knowledge research,
load the complete shipped persona from `.agents/camel-kit-personas/` and use `invoke_subagent` with `camel-reviewer`.
Validate version/runtime/BOM bindings before dispatch, pass variable-length inputs as separate canonical envelopes,
and corroborate returned evidence before using it in the design. The parent writes the design and owns approval.
