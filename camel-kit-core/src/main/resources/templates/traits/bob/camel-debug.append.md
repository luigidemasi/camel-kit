## Agent Optimization: IBM Bob

Switch to `camel-debug-mode` before starting the diagnostic workflow. Keep diagnosis and any fix in the same session
because Bob 1 has no subagent isolation. In diagnosis-only use, wait for the user to request the fix. For a direct fix
invocation, continue with shipped in-scope repairs; pause only for an exact loaded-content-derived action that requires
action-specific confirmation under `camel-debug/SKILL.md`.
